{
  "name": "BookBridge",
  "postCreateCommand": "npm install -g pnpm && pnpm install",    
  "postStartCommand": "pnpm prisma generate && prisma db push && dev",
  "remoteEnv": {
    "DATABASE_PRISMA_URL": "file:./db.sqlite",
    "DATABASE_URL_NON_POOLING": "file:./db.sqlite"
  }
}